# IEEE-CIS 欺诈检测机器学习项目规划

## 0. 项目契约

### 项目目标

本项目的第一目标是系统掌握经典机器学习，第二目标是产出一个对机器学习实习、风控算法实习、数据挖掘实习和研究生推免有帮助的硬核项目。

项目不以一周内完成或 Kaggle 排名为目标，而以“能够理解、解释、复现和改进完整机器学习流程”为目标。

### 已确定方向

- 项目主题：IEEE-CIS 电商交易欺诈检测
- 数据文件：`train_transaction.csv`、`train_identity.csv`
- 标签：`isFraud`
- 主要参考：`ageron/handson-ml3`
- 重点领域：表格机器学习、金融风控、类别不平衡、时间验证、模型解释
- 首选主模型：梯度提升模型/XGBoost；必须先完成可解释基线

### 明确不做的事

- 不重新做已经完成的信用卡违约预测项目；
- 不为了“看起来高级”直接堆深度学习、图神经网络或复杂集成；
- 不只追求一个漂亮的 AUC；
- 不使用无法解释的代码和参数；
- 不提前填写虚构的指标、样本数量或提升比例；
- 不把 Kaggle 竞赛技巧直接包装成真实生产风控能力。

## 1. 项目问题定义

使用交易发生时可以获得的交易信息和身份信息，预测一笔电商交易是否可能是欺诈交易，并研究：

1. 哪些信息对欺诈识别有帮助；
2. 随机切分和时间切分的结果是否不同；
3. 类别不平衡如何影响模型和评价指标；
4. 哪些特征可能造成数据泄漏；
5. 在有限人工审核预算下，模型能捕获多少欺诈交易。

最终报告必须说明：该数据集来自公开竞赛，特征包含匿名字段，结论不能直接等同于真实银行生产环境结论。

## 2. 学习主线

以 `handson-ml3` 为学习教材，但只学习与项目直接相关的内容：

| 学习主题 | 主要内容 | 项目落地 |
|---|---|---|
| 端到端流程 | 问题定义、数据探索、测试集 | 项目总体流程 |
| 分类 | 逻辑回归、混淆矩阵、Precision、Recall | 逻辑回归基线 |
| 训练模型 | 损失、正则化、梯度下降的基本直觉 | 理解模型训练 |
| 决策树 | 切分、过拟合、树深度 | 决策树基线 |
| 集成学习 | 随机森林、梯度提升 | 主模型比较 |
| 模型评估 | 交叉验证、阈值、误差分析 | 风控评价体系 |
| 模型解释 | 特征重要性、局部错误分析 | 业务解释和报告 |

每个阶段都必须先理解概念，再写代码，再解释运行结果。

## 3. 分阶段路线

### 阶段 A：基础能力补齐

目标：能够独立解释一个监督学习项目的基本组成。

掌握：

- 特征、标签、样本；
- 训练集、验证集、测试集；
- 欠拟合、过拟合；
- 数据泄漏；
- 分类阈值；
- Precision、Recall、F1、ROC-AUC、PR-AUC；
- Pipeline 和交叉验证。

验收：能够用自己的话解释为什么欺诈检测不能只看 Accuracy。

### 阶段 B：数据审计与数据理解

目标：在不训练模型的情况下，读懂数据并识别风险。

完成：

- 检查两个训练文件的大小、字段和主键；
- 按 `TransactionID` 合并交易表和身份表；
- 分析标签比例；
- 分析缺失值、重复值和异常值；
- 识别数值变量、类别变量和匿名变量；
- 明确 `TransactionDT` 的时间含义；
- 形成数据字典和数据质量报告。

产出：`01_data_audit.ipynb`、`data/README.md`。

### 阶段 C：可靠的基线模型

目标：先建立能够解释和复现的基线。

模型顺序：

1. 规则或简单多数类基线；
2. 逻辑回归；
3. 决策树；
4. 随机森林；
5. 梯度提升/XGBoost。

每个模型都记录：数据版本、特征版本、切分方式、参数、运行时间和评价指标。

产出：`metrics.csv`、模型比较表、混淆矩阵和 PR 曲线。

### 阶段 D：特征工程与实验设计

目标：理解特征如何影响模型，而不是盲目增加字段。

尝试：

- 交易金额变换；
- 时间特征；
- 缺失值数量；
- 类别变量频率编码；
- 交易相关的行为统计；
- 交易表与身份表的特征组合。

进行消融实验：

- 仅交易基础特征；
- 基础特征加行为特征；
- 再加入身份表特征；
- 去除疑似泄漏字段后的结果。

每次实验只改变一个主要因素，避免无法解释结果来源。

### 阶段 E：时间验证与数据泄漏审计

这是本项目的核心硬核部分。

至少比较：

- 随机切分；
- 按 `TransactionDT` 的时间切分；
- 不同时间窗口的验证结果。

重点检查：

- 身份信息是否在预测时可获得；
- 是否使用了标签衍生字段；
- 是否在全量数据上计算了频率或统计特征；
- 预处理是否只在训练集上拟合；
- 是否因随机切分导致相似交易泄漏到验证集；
- `isFlaggedFraud` 等规则结果是否应该排除。

最终报告必须有一节专门讨论泄漏和泛化风险。

### 阶段 F：模型解释与业务评价

目标：把模型分数转化为风控决策语言。

完成：

- 特征重要性；
- 排列重要性或 SHAP；
- 漏报欺诈交易分析；
- 误报正常交易分析；
- 不同阈值下的 Precision-Recall 权衡；
- Top 1%、Top 5% 风险交易捕获率；
- 人工审核预算有限时的模型表现。

### 阶段 G：工程化与展示

目标：让别人能够理解并重新运行项目。

完成：

- 清理 Notebook；
- 抽取 `src/` 训练和评估脚本；
- 保存关键指标和图表；
- 编写 README；
- 写明环境、数据获取、运行顺序和限制；
- 完成项目报告和简历表述。

## 4. 推荐仓库结构

```text
ieee-cis-fraud-detection/
├── README.md
├── README_ML_PROJECT_HANDOFF.md
├── PROJECT_PLAN.md
├── requirements.txt
├── data/
│   └── README.md
├── notebooks/
│   ├── 01_data_audit.ipynb
│   ├── 02_preprocessing_and_features.ipynb
│   ├── 03_baseline_models.ipynb
│   ├── 04_model_comparison.ipynb
│   └── 05_error_analysis.ipynb
├── src/
│   ├── data_loader.py
│   ├── preprocessing.py
│   ├── features.py
│   ├── train.py
│   └── evaluate.py
├── results/
│   ├── figures/
│   ├── metrics.csv
│   └── feature_importance.csv
└── reports/
    └── project_report.md
```

## 5. 项目验收标准

### 学习验收

- 能解释监督学习项目的完整流程；
- 能解释逻辑回归、决策树、随机森林和梯度提升的基本思想；
- 能解释为什么需要时间切分；
- 能解释数据泄漏；
- 能根据业务目标选择评价指标和阈值；
- 能独立阅读和修改主要代码。

### 项目验收

- 至少三个模型完成公平比较；
- 至少包含随机切分和时间切分对比；
- 至少完成一组特征消融实验；
- 报告 ROC-AUC、PR-AUC、Recall、Precision、F1；
- 报告 Top-K 风险捕获率；
- 有错误分析和特征解释；
- 有数据泄漏审计；
- 能从原始数据重新运行主要流程；
- 所有数字都来自实际运行结果。

## 6. 时间安排原则

不设置“一周必须完成”的硬截止时间，建议按 6—8 周推进：

- 第 1 周：基础概念和项目环境；
- 第 2 周：数据审计与数据理解；
- 第 3 周：逻辑回归和评估指标；
- 第 4 周：决策树、随机森林和梯度提升；
- 第 5 周：特征工程与消融实验；
- 第 6 周：时间验证、泄漏审计和误差分析；
- 第 7 周：模型解释和业务评价；
- 第 8 周：工程化、README、报告和简历。

如果某一阶段没有真正理解，不因为时间表而跳过；如果基础流程稳定，再增加 XGBoost、SHAP 或更复杂实验。

## 7. 后续协作规则

1. 指导者负责设计学习路径、解释概念、提出任务和检查结果，不代替学习者完成核心工作；
2. 每次只推进一个明确的小步骤；
3. 每个步骤开始前，先说明“要做什么、为什么做、输入是什么、预期输出是什么”；
4. 优先给出提示、思路或代码骨架，由学习者先尝试；只有在明确要求或多次卡住时，才提供更完整的代码；
5. 开始检查或评价代码前，必须先读取当前实际文件；
6. 不直接一次性生成全部项目代码；
7. 每段代码都说明输入、输出、作用和原因；
8. 关键步骤完成后，学习者需要能够用自己的话解释其目的和结果；
9. 每完成一个阶段，都记录实际结果和未解决问题；
10. 不因长上下文丢失而改变项目主题；
11. 如果未来需要调整方向，必须明确记录“为什么调整、调整了什么、哪些成果保留”；
12. 后续协作者应先阅读本文件、`README_ML_PROJECT_HANDOFF.md` 和当前代码，再开始工作。

## 8. 当前状态

- 项目方向：已确定为 IEEE-CIS Fraud Detection；
- 学习主线：`handson-ml3` 经典机器学习；
- 旧信用卡违约项目：不作为本项目主体；
- 数据集：五个官方 CSV 已下载到 `data/raw/`；
- 环境：项目 `.venv` 可用，已验证 pandas；
- 数据审计：已检查训练/测试表结构、主键唯一性、缺失率、身份表覆盖率和训练/测试时间范围；
- 验证策略：已采用 `TransactionDT` 前 80% 训练、后 20% 验证的时间切分；官方 test 保持未参与模型选择；
- 特征版本 V1：交易金额、金额对数、交易小时、交易日、商品类型、卡类型和邮箱域名；
- 已完成模型：逻辑回归基线、受限单棵决策树、随机森林和 XGBoost；各实验均记录在 `results/metrics.csv`，并固定使用相同的时间验证合同进行主线比较；
- 已完成评价：ROC-AUC、PR-AUC、Precision、Recall、F1、混淆矩阵、阈值分析和 Top-K 欺诈捕获率；
- 特征实验：V2 的训练期频率编码使 XGBoost PR-AUC 达到 0.208093；V3 在 V2 上加入无缺失匿名数值 C 组（`C1`～`C14`），PR-AUC 提升至 0.465917；V4 加入低基数类别 M 组（`M1`～`M9`）仅带来极小增益，因此不纳入主特征版本；
- 当前主候选特征版本：V5 = V3 + D 组数值字段（`D1`～`D15`）+ 各 D 字段的显式缺失指示器。后期时间窗口中，采用 early stopping 确定的 414 棵树后，ROC-AUC 为 0.907598，PR-AUC 为 0.491739；Top 1%、5%、10% 审核预算下的欺诈捕获率为 24.78%、55.22%、69.12%；
- 时间稳定性检查：随机切分 V5 的 PR-AUC 为 0.618253，明显高于时间切分，已作为随机切分的乐观参考而非模型选择依据；前 60% 训练、60%～80% 验证的 early stopping 给出最佳树数 414，说明存在时间漂移，继续以时间切分作为主评价；
- 模型解释：按 split gain 汇总，业务类别特征占 37.42%、匿名 C 组占 37.08%、D 原值占 11.05%、D 缺失指示器占 8.34%。匿名 C/D 和缺失模式具有强信号，但业务语义和线上可得性仍未验证；
- 误差分析：后期时间窗口、阈值 0.5 下，V5 的 TN/FP/TP/FN 为 104541/9503/2857/1207。`ProductCD=W` 的欺诈捕获率为 48.95%，显著低于 `C` 的 89.80%，但两组的误报率也不同；该结果仅用于定位后续分析方向，尚不据此制定分组阈值；
- 结果与图表：已生成结果汇总表、特征组重要性表，以及模型比较、特征消融、验证策略、审核容量和特征依赖图，存于 `results/` 与 `results/figures/`；
- identity 受控审计与消融：已完成。早期/后期 identity 覆盖率为 25.50%/20.12%；有 identity 交易的欺诈率为 7.55%/9.34%，高于无 identity 交易的 2.13%/1.96%。第一轮仅加入低缺失、低基数的 `id_12`、`id_15`、`id_35`～`id_38` 和 `DeviceType`；V6 的 ROC-AUC/PR-AUC 为 0.906067/0.491420，均低于 V5 的 0.907598/0.491739，Top 5% 捕获率也从 55.22% 降至 54.55%，故不保留 identity 字段。高基数 `DeviceInfo`、`id_31` 与高缺失字段不再继续扩展；
- 数据泄漏与线上可得性审计：已由 `27_leakage_audit.py` 固化。已验证：标签和 `TransactionID` 未进入 V5；频率编码仅由训练期拟合；数值/类别预处理在训练集 fit；官方 test 未参与模型选择。`isFlaggedFraud` 不属于 IEEE-CIS 的交易表，已明确为不适用，不能与其他欺诈数据集混淆。C/D 匿名字段、identity 字段和频率特征的真实线上生成时点均无法从公开数据确认，必须在 README 和报告中明确为限制；
- 工程化主训练流程：已完成。V5 已抽取为 `src/` 的数据读取、特征构造、预处理建模、评价与训练入口模块；运行 `python -m src.train` 可复现后期时间验证，复跑结果与 V5 基准一致（PR-AUC 0.491739，Top 5% 捕获率 55.22%）。全部 `01_`～`27_` 编号脚本均保留为学习和实验轨迹；`.gitignore` 排除了虚拟环境与原始数据；
- 当前下一步：使用全量带标签训练数据训练固定的 V5 流程，并对官方 test 生成 submission。该步骤只演示从训练到无标签推理的可复现链路；官方 test 无标签，不用于选择模型或报告泛化指标。







ps:
损失函数：
\[
\text{Loss}=\sum_i l(y_i,\hat y_i)+\Omega(f)
\]其中 \(l\) 是预测误差，\(\Omega(f)\) 是模型复杂度惩罚。
Gain：
\[
\text{Gain}
=
\text{分裂前Loss}
-
\text{分裂后Loss}
\]Gain 越大，说明这次节点分裂越有价值。



ROC-AUC
ROC 曲线横轴是误报率，纵轴是真正率：
\[
TPR=\frac{TP}{TP+FN}
\]\[
FPR=\frac{FP}{FP+TN}
\]ROC-AUC 是 ROC 曲线下面积：
\[
\mathrm{ROC\text{-}AUC}
=
\int_0^1 TPR(FPR)\,d(FPR)
\]看模型能否把欺诈样本的概率排在正常样本前面，适合衡量整体排序能力。

PR-AUC
PR 曲线横轴是 Recall，纵轴是 Precision：
\[
Recall=\frac{TP}{TP+FN}
\]\[
Precision=\frac{TP}{TP+FP}
\]PR-AUC 是 PR 曲线下面积：
\[
\mathrm{PR\text{-}AUC}
=
\int_0^1 Precision(Recall)\,d(Recall)
\]看模型找出的欺诈样本是否足够准确，重点关注少数类，适合不平衡的欺诈检测。


12:06
